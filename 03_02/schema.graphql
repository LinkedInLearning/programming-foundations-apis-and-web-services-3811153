# Defines types, queries, and mutations.
type Book {
    title: String
    author: String
}

type Query {
    book(title: String!): Book
    books: [Book]
}

type Mutation {
    addBook(title: String!, author: String!): Book
}
